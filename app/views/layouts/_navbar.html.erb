<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-9">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a ng-if="!userLoggedIn" class="navbar-brand" href="/">Survey</a>
      <a ng-if="userLoggedIn"  class="navbar-brand" href="#/homes">Survey</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-9">
      <ul class="nav navbar-nav">
        <li><a href="#/survey_profiles"> Survey List</a></li>
        <li ng-if="userLoggedIn"><a href="javascript:void(0);" data-action="open" survey-profile-popup> New Survey</a></li>
        <div id="survey_type_popup"></div>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <li ng-if="!userLoggedIn"><a href="#/users/sign_in">Sign in</a></li>
        <li ng-if="!userLoggedIn"><a href="#/users/sign_up">Sign up</a></li>
        <li ng-if="userLoggedIn"><a href="javascript:void(0);">{{currentUser.username}}</a></li>
        <li ng-if="userLoggedIn">
          <a href="javascript:void(0);" ng-click="deleteSession();">Sign out</a>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div growl class="growl-notification"><%= alert %></div>
